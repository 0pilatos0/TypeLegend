/**
 * Divides two numbers.
 * @param a The dividend.
 * @param b The divisor.
 * @returns The quotient of the two numbers.
 */
function divide(a: number, b: number): number {
  return a / b;
}

export { divide };
